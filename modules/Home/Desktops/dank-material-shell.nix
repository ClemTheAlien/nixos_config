{
programs.dankMaterialShell = {
  enable = true;

  default.settings = {
    dynamicTheming = true;
    # Add any other settings here
  };

  default.session = {
    # Session state defaults
  };
};
}